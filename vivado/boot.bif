// boot.bif — Zynq-7000 QSPI boot image format
//
// Combines the FSBL and PL bitstream into a single boot image.
// The Zynq BootROM loads the FSBL from QSPI flash, then the FSBL
// configures the PL with the bitstream.
//
// Usage: bootgen -image boot.bif -arch zynq -o BOOT.bin -w

the_ROM_image:
{
    [bootloader] boot/fsbl/executable.elf
    mandelbrot/mandelbrot.runs/impl_1/mandelbrot_top.bit
}
